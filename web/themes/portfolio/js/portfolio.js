/**
 * @file
 * portfolio behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.portfolio = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
